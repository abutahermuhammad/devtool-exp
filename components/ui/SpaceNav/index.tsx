import { ReactElement } from "react";
import S from './SpaceNav.module.sass'




export default function SpaceNav({className, children }: {className: string, children: ReactElement}) {
    return (
        <div className={S.__area}>

        </div>
    )
}